mod loops;
mod enums;
mod constants;
mod tuples;

fn main() {
    //loops::infinite_loop();
    //loops::while_loop();
    //loops::for_loops();

    //enums::var_enum();

    constants::testing_constant();
    tuples::define_tuples();

    println!("Hello, world!");
}
